{:paths ["src" "test"]
 :tasks
 {test {:doc "Run kurashimori murakumo actor boundary tests"
        :task (do
                (require 'clojure.test
                         'kurashimori.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'kurashimori.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
